<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.coloredmoon.com.chicajimenezemilio">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permisssion.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".SplashScreen"
            android:configChanges="orientation"
            android:label="FarmaSearch"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation"
            android:label="@string/app_name"
            android:screenOrientation="portrait" />
        <activity
            android:name=".CargaFarmacia"
            android:label="@string/title_activity_carga_farmacia"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="utilidades.MapaComoLLegar"
            android:configChanges="orientation"
            android:label="Mapa para llegar a la farmacia"
            android:screenOrientation="portrait" />
        <activity
            android:name=".CargaProductos"
            android:label="@string/title_activity_carga_productos"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".CargaCesta"
            android:label="@string/title_activity_carga_cesta"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".CargaProducto"
            android:label="@string/title_activity_carga_producto"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".CargaInicioSesion"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".CargaRegistro"
            android:theme="@style/AppTheme"/>
        <activity
            android:name="utilidades.Busqueda"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".CargaProductosPorDpto"
            android:label="@string/title_activity_carga_producto"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".CargaDepartamentos"
            android:label="@string/title_activity_carga_producto"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".CargaContacto"
            android:label="@string/title_activity_carga_producto"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".CargaReservas"
            android:label="@string/title_activity_carga_producto"
            android:theme="@style/AppTheme" />
    </application>

</manifest>
